#!/usr/bin/python3
# This script defines a function copy_list(l) that returns a copy of a list.
# The input list can contain any type of objects.
# The function uses list slicing to create a new list.
def copy_list(l):
    return l[:]
